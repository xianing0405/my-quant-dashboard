# 投研数据监控台（web_dashboard.py）与配套脚本的 Python 依赖
# 生成于 2026-09-16；版本号为本地已验证版本，可复现部署。
# 若云端 Python 版本差异导致解析失败，可将 "==" 放宽为 ">="。

# 核心 Web 框架
streamlit==1.58.0

# 数据可视化
plotly==6.7.0
matplotlib==3.11.0

# 数据处理
pandas==3.0.3

# HTTP 请求（大模型接口调用 / 数据拉取）
requests==2.34.2

# 行情数据（预留：当前代码未直接 import，按需求包含）
yfinance
